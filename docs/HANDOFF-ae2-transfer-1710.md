# Agent 交接：Forge 1.7.10 AE2 传输核心

交接时间：2026-09-12 12:19（Asia/Shanghai）。

## 下一会话任务与授权

用户已经确认 intent-completion 的方向及完整范围，最后明确要求：“确认。先不实施，产出实施计划并 handoff 交接给下个agent实施”。本轮仅制作交接，不创建或唤醒执行 Agent。

下一会话受领任务后实施 [PLAN-ae2-transfer-1710.md](https://github.com/djp440/freecam-interaction/blob/main/docs/PLAN-ae2-transfer-1710.md)。它是需求、玩法选择、工程依据、步骤和验收的唯一基线；本文不重复其清单。不必重新走已完成的两道确认；如接手时用户仍只要求阅读交接，先遵守该当轮指令。

## 项目入口与当前状态

- 实施目录：当前工作区的相邻 `../上帝视角建造-forge1710`；分支 `main`。先阅读目标树的 [AGENTS.md](https://github.com/djp440/freecam-interaction/blob/main/AGENTS.md)、[MEMORY.md](https://github.com/djp440/freecam-interaction/blob/main/MEMORY.md)，再读计划。
- 当前目录为 1.21.1 入口树；本次不在这里实现功能。目标版本和两树基线提交见计划第1、3节。
- 本轮只做只读源码/官方资料核对，编写计划、交接和两树记忆。没有功能、配方、材质、模型或测试代码改动，没有下载/安装依赖、构建、运行游戏、Git提交或实机验收。
- 本轮开始时，入口树已有 AGENTS.md、MEMORY.md 未提交修改；目标树已有未跟踪 `.diagnose-tmp/`。这些不是本轮创建的，保留不动。
- 现有施工源码被 `build/` 忽略规则排除，文件实际存在而未被 Git 跟踪。必须使用现有目标工作树，先处理计划第3.1节的可复现性风险，不要只从 HEAD 新建干净检出后认定“蓝图未实现”。

## 接手顺序

1. 核对最新工作树和文档，将计划第2节作为已确认契约，第3节作为证据，第4节作为执行顺序。
2. 优先完成计划 A 阶段：真实 AE2 JAR 的映射、可选类加载及远端终端宿主验证。四终端存在具体类型约束，不能仅注册普通无线处理器就宣称全功能完成。
3. 依次执行绑定、会话与终端、施工结算、美术资源和定向验收。尤其注意主任务入口与备用执行器不同，必须修改真实主路径。
4. 按计划第5节记录通过及未验证项目，更新计划状态和两树项目记忆后交付，不把本交接的只读核对当作功能测试结果。

## Suggested Skills

下一 Agent 根据执行阶段使用 Skill 工具加载；没有该工具时，定位并读取相应 SKILL.md，不需要为本任务安装新技能。

- `codebase-memory`：需要查找结构和调用关系时使用；图谱不可用或覆盖不足时回到真实源码，特别注意被忽略的施工目录。
- `imagegen`：制作核心及传输器所需原创位图时使用；读取后遵守当时工具与导出规则，不能沿用旧交接中的过期工具名。
- `minecraft-gameplay`：实际进入游戏做贴图、模型、四终端及材料供给验证时使用；此计划不代表本轮已经进行游戏操作。
- `intent-completion`：仅在发现会改变已确认范围的增量需求时使用，不重开已经完成的确认流程。
- `handoff`：需要再次交接时使用，引用计划及已产生的实现记录，避免复制整份规格。

## 验证与资料位置

官方旧版文档、精确 tag、源码链接及本地 JAR 校验值集中在计划第3节；后续待核验事项见第6节。不要用现代 AE2 的同名概念/API 替代 rv3，也不要把原生无线距离/电池限制重新加回用户已确认的自定义行为。

常用脚本、日志规则和检查矩阵均见计划第4、5节。本轮只核对文档一致性、链接目标和文件差异；不运行功能冒烟。下次实施时仍按项目要求完成基础冒烟和必要目视检查。

2026-09-12 12:26：六份本轮涉及文档的 UTF-8 解码、已有本地 Markdown 链接及两份 MEMORY.md 时间倒序检查通过；两树 `git diff --check` 无错误，只有换行提示。本轮状态仅增加计划/交接及修改项目文档；未改变功能源码。
