# 脚手架冒烟验收

## 交互范围显示（2026-09-08）

- 已确认范围：可建造的上帝视角中显示完整、淡化、受地形遮挡的 16×16×16 青白色线框；菜单、覆盖层、F1、退出及服务器不支持交互时隐藏，中键旋转期间保留。
- 2026-09-08 22:49：`scripts/dev.ps1 smoke` 通过构建、交互几何及相机自检，日志 `logs/tools/2026-09-08 22-49-05.log`；新增断言覆盖正负坐标及半格吸附临界点，`git diff --check` 通过（仅换行提示）。
- 待游戏内目视体验：线框与实际可交互方块外沿一致；地形遮挡正确；平移、旋转、FOV 和窗口变化时不偏移或闪烁；选中面呼吸高亮仍清晰。
- 未执行玩法 E2E 或进入存档，不将编译和数学自检视为视觉验收通过。

## 光标建造交互（2026-09-08）

- 已确认范围：玩家脚底坐标为中心的 16 格立方体，选中面呼吸高亮，原版挖掘/使用与标准 Mod 事件链；不含实体操作、隔墙、批量建造和撤销。
- 2026-09-08 22:09：`scripts/dev.ps1 smoke` 通过构建、交互几何自检与相机自检；日志 `logs/tools/2026-09-08 22-09-50.log`。覆盖整数/半整数/负数中心的 16 格跨度、边界排除、NaN/无穷、透视射线中心/边缘/旋转方向、立方体六面、半砖与楼梯遮蔽面。
- 2026-09-08 22:11：通过 `scripts/dev.ps1 client` 实际启动并目视确认主菜单，正常关闭且 Gradle 返回成功；未进入或修改存档，未出现启动时 Mixin 加载错误。日志 `logs/tools/2026-09-08 22-10-05.log`。
- 2026-09-08 22:12：补充服务端延迟挖掘取消与使用请求日志后，最终冒烟再次通过，日志 `logs/tools/2026-09-08 22-12-41.log`；`git diff --check` 通过，仅有 Windows 换行提示。
- 待游戏内体验：生存模式按住挖掘的进度/工具/耐久/掉落，创造模式连续挖掘节奏；切换目标、快捷栏、松键和越界应中止旧目标。
- 待游戏内体验：原版箱子/熔炉/工作台在立方体角落可用；Shift 放置、副手、可替换方块和边界放置；目标被拆除或玩家越界后容器关闭。
- 待游戏内体验：光标与六个面、台阶/楼梯的高亮一致；玻璃遮挡不穿透；不同 FOV、窗口尺寸和画质下不偏移、不闪烁争深度。
- 待游戏内体验：打开菜单/容器、失焦、中键旋转时不误操作，返回需松开后重新按键；死亡、换维度、断线和退出恢复原版范围。
- 未进行玩法 E2E、独立服务器联机、保护插件或第三方 Mod 兼容验证；以上自检和启动检查不能替代这些验收。

## 用户路径与成功判据

1. 按 README 安装 JDK：安装成功，重复执行复用；系统默认 Java 不变。
2. 执行 `scripts/dev.ps1 smoke`：生成 JAR；入口、日志模块、中文名称和 Minecraft 精确版本范围正确；日志自检通过。
3. 输入非法操作：显示允许的操作并返回非零退出码，留下工具日志。
4. 执行 `scripts/dev.ps1 client`：真实客户端进入主菜单，在 Mods 中找到“上帝视角建造”；初始化日志写入独立文件。

## 隔离与范围

仅使用项目新建的 `run/client` 开发目录和官方依赖，不接触已有游戏实例或存档。不进行玩法 E2E；服务端只提供启动入口，不代为接受 EULA。

## 结果

结果：路径 1、2、3 已完成。路径 4（真实客户端进入主菜单并查看 Mods）未执行；用户未要求 E2E，且最小冒烟已通过。工具凭证位于 `logs/tools/2026-09-08 20-12-07.log`。

## 上帝视角输入改造（2026-09-08）

- 2026-09-08 21:38：用户确认“验收通过，提交”。本轮功能按用户反馈验收通过；以下待体验清单保留为回归参考，不代表用户提供了逐项测试凭证。

- 已执行 `scripts/dev.ps1 smoke`：构建、JAR 内容、相机访问转换器及运动逻辑自检通过，凭证 `logs/tools/2026-09-08 21-29-04.log`。
- 自检覆盖前进/右移/转向后的移动方向、斜向等速、零输入、帧率一致性、长帧限幅和非法数值。
- 待游戏内体验：G 进入，WASD 平移但玩家不移动；中键旋转但玩家不转头；数字键/滚轮选物品；E 背包关闭后仍在原相机位置。
- 待游戏内体验：Esc 打开菜单并进入设置，返回后模式保留；G 和右上角 X 分别退出并恢复原视角。
- 待游戏内体验：按住中键时打开菜单/切走窗口再返回不持续拖拽；死亡、跨维度、断线清理状态；缩放窗口及改键后 HUD 提示正常。
- 本次未执行真实客户端交互或 HUD 目视验收，不将构建和运动自检视为玩法通过。
