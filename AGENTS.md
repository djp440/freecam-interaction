# 项目说明

- 开源仓库与分支（2026-09-14）：项目对外名称统一为“自由视角交互”，GitHub 仓库名为 `freecam-interaction`，采用 MIT License。Forge 1.7.10 是默认 `main` 与主要开发版，NeoForge 1.21.1 是 `neoforge-1.21.1` 次要维护版；1.21.1 为兼容现有存档与协议保留技术标识 `godview_build`。

- 蓝图缺料与任务存档（2026-09-12）：用户已确认相邻 Forge 1.7.10 蓝图补入普通 ME 终端后完工。现增加当前施工步骤无法取得的物品名称与数量提示，并将线上任务表接入已有 `.task` 存储；重进恢复待施工、续建复核已建部分，完成/取消不再恢复，跨存档清理缓存。旧版已丢失且未落盘的任务无法恢复。Forge 与 lwjgl3ify 准备冒烟通过，新版已同步实例，实际重进续建待确认；1.21.1 对应功能待实现。

- AE2 传输核心（2026-09-12）：相邻 Forge 1.7.10 工作树已接续完成 AE2 rv3-beta-6 可选联动、传输器绑定与多目标选择、四类远端终端、背包/网络导航和蓝图 ME 补缺/实物回滚；客户端不强载远端区块。用户已实机确认绑定、ME 供料及远端终端功能正常；自由视角 E 由服务端按实时绑定状态选择默认合成终端或原版背包，虚拟终端构造期可兼容 AE2 父类对 `getProxy()` 的早期调用，跨页导航以方形按钮在容器左侧纵排。原生 Forge 冒烟与 lwjgl3ify 3.0.33 + Java 25 准备冒烟已通过，断线/断网回滚尚待人工验收。计划入口为 `HANDOFF-ae2-transfer-1710.md`，完整基线在 `../上帝视角建造-forge1710/PLAN-ae2-transfer-1710.md`；1.21.1 对应功能待实现。

- 蓝图系统（2026-09-11）：相邻 Forge 1.7.10 工作树已依据 `PLAN-blueprints-1710.md` 规范全量实现并完成自动化冒烟验证（包含法杖 4 槽升级容器、蓝图核心、个人蓝图存储、三档任务权限、拓扑施工调度与物料守恒结算）；当前 1.21.1 对应功能待实现。

- 通用交互掉落入包（2026-09-10）：相邻 Forge 1.7.10 工作树已实现按服务端同步交互上下文归属的掉落直接入包，覆盖挖掘、右键工具拆卸、实体交互/击杀、钓鱼及 AE2 ME 线缆/部件拆卸；背包余量安全落地并保留 NBT。当前 1.21.1 对应功能待实现。

- 科技与辅助 Mod 开发环境（2026-09-10）：相邻 Forge 1.7.10 工作树的原生与 lwjgl3ify 开发实例均已安装 IC2 2.2.828-experimental、AE2 rv3-beta-6、CodeChickenCore 1.0.7.47、NotEnoughItems 1.0.5.120 与 GregTech 5.09.31 Unofficial；可用其 `scripts/tech-mods.ps1` 恢复。

- 法杖材质（2026-09-10）：相邻1.7.10工作树已补充32×32透明PNG（普通蓝／高级红／创造紫），详见其 `art/wands/PROMPTS.md`；本工作树1.21.1功能仍待实现。

- 法杖功能（2026-09-10）：已确认范围、仅完成方案交接；1.7.10 执行方案在相邻工作树 `../上帝视角建造-forge1710/PLAN-wands-1710.md`，入口 `HANDOFF-wands-1710.md`。1.21.1 对应功能待实现，不能把计划视作已实现行为。

Minecraft 1.21.1 / NeoForge 的“自由视角交互”次要维护版，已实现高位第三人称观察与范围内光标建造交互。使用简体中文和 UTF-8。

- 默认 G 切换模式（可在控制设置中改键），右上角 X 按钮退出；Esc 打开原版菜单，不退出模式。客户端代码位于 `src/main/java/local/godviewbuild/client`。
- WASD 相对视角水平平移，中键拖拽旋转；保留原版数字键/滚轮快捷栏和 E 背包。模式状态独立于 Screen，菜单期间停止相机输入，死亡/世界切换/断线清理。
- 模式只改变相机与界面，不传送玩家、不暂停世界；使用镜头局部连续碰撞避障（方案 A）与防下潜，退出恢复原视角。
- 交互以玩家脚底坐标为中心，边长 16，按方块中心判定，下界包含、上界不包含；相机光标射线不跳过前方遮挡。放置落点也校验范围。
- `GodviewInteraction` 管理客户端/服务端模式及范围，Mixin 仅替换模式内距离、放置边缘与输入入口；保留原版计时、权限和 NeoForge 事件。联机未协商支持时仅观察。
- `GodviewSelection` 使用渲染矩阵选取，按实际形状外露面渲染；中键旋转、界面、失焦后需松开交互键再按下。Shift 保留辅助使用，不允许实体操作。

- Java 21，依赖版本固定，不提前创建跨版本抽象。
- 使用 `pwsh -File scripts/dev.ps1 check|build|client|server|smoke`；安装用 `scripts/setup.ps1`。
- 不修改系统默认 Java，脚本仅在当前进程选择项目 JDK。
- 完成修改执行最小冒烟，不默认执行全量 E2E。
- `smoke` 包含无测试框架的相机运动与交互几何自检；交互检查使用主源码依赖类路径，不另引入测试框架。相机锚点设置后采用镜头局部碰撞避障（方案 A），阻断原版第三人称长连线回缩。
- Mod 日志在运行目录 `logs/godview_build`，工具日志在项目 `logs/tools`；Windows 文件名使用 `yyyy-MM-dd HH-mm-ss.log`。
- 保留 Minecraft/Gradle 原生日志，不删除用户存档，不自动接受 EULA。
- 维护 MEMORY.md，记录精确到分钟的决定及验证结果。

- 1.7.10 移植处于调研阶段：用户计划支持 Forge 1.7.10 与 lwjgl3ify；现有工程仍为 1.21.1，目标依赖版本与整合包兼容范围尚未确定，不能将 lwjgl3ify 兼容等同于整个 GTNH 整合包兼容。
